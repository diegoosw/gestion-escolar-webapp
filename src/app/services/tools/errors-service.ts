import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class ErrorsService {
  
}
//agregar código a errors, validator, administradores-service, registro-admin.ts, notification-service, maestros-service, registro-maestros.ts, registro-maestros.scss
//instalar xampp, postman pa validar apis, pyhton (una version antes a la última)