import { Injectable } from '@angular/core';
//token es una cadena de texto que se utiliza para autenticar a un usuario en una aplicación web, generalmente se genera después de que el usuario inicia sesión correctamente y se almacena en el cliente (por ejemplo, en localStorage o cookies) para ser enviado con cada solicitud al servidor y verificar la identidad del usuario.

@Injectable({
  providedIn: 'root',
})
export class AuthServices {
  
}
